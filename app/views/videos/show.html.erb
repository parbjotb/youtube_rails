<% content_for(:html_title) { @video.title } %>
<%# This line above puts the value of video.title inside html_title %>

<%# Saying @video will have the show page show an individual listing %>
<%= render partial: 'video', object: @video, locals: {show_link: false, single_page: true } %>