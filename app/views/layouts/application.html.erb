<!DOCTYPE html>
<html>
  <head>
  <%# this line below makes the browser say the name of the video
  only if we are on a video show page, not the index %>
    <title><%= yield(:html_title) + " - " if content_for?(:html_title) %>Trending Youtube Songs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <section class="section">
    <div class="container-fluid">
         <h1 class="display-1"><%= link_to "Popular Youtube Uploads", root_path %>Will change style!!!</h1>

         <%= form_with(url: search_videos_path, method: :get, local: true) do%>
          <div class="searcharea d-flex">
            <div class="lead">
              <%= text_field_tag(:keywords, nil, class: "searchtext", placeholder: 'Search Youtube Data') %>
            </div>
            <div class="lead">
              <%= submit_tag("Search", class: "btn btn-primary") %>
            </div>
          </div>
          <% end %>
    </div>
    <%= yield %>

    <%= render partial: 'common/footer'%>
  </section>
  </body>
</html>
